<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Define log paths -->
    <property name="LOG_DIR" value="./logs" />
    <property name="LOG_FILE" value="${LOG_DIR}/application.log" />

    <!-- Console Appender with colors -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Daily Rolling File Appender with Max Size limit -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Daily rollover with index -->
            <fileNamePattern>${LOG_DIR}/archived/application-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- Max file size before rolling -->
            <maxFileSize>10MB</maxFileSize>
            <!-- Keep 14 days of history -->
            <maxHistory>14</maxHistory>
            <!-- Limit total archives to 2GB -->
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Application logger -->
    <logger name="com.kakak.kakak_backend" level="INFO" />

    <!-- Spring Framework logger -->
    <logger name="org.springframework" level="INFO" />

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
